# VS Code を開く

1. 開く対象を決める。
2. 対象のファイルまたはディレクトリが明示されている場合はそれを開く。
3. 対象が明示されていない場合は、カレントディレクトリを開く。
4. `code` コマンドが使える場合は、対象パスを指定して実行する。
5. `code` コマンドが使えない場合は、`open -a "Visual Studio Code"` で対象を開く。
6. 終了コードと標準エラー出力を確認する。

対象パスが存在しない場合は、その旨を伝えて対象を確認する。