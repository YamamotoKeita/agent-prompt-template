最終的に以下のゴールを目指すための足掛かりとして、スコープを絞ったMVPを最初に作る。

1. AIエージェントに要件を伝えたら、全自動で開発してくれること。
2. 要所においてはエージェントが自発的に人間への確認や質問を行い、勝手な想定で作業を進めないこと。
3. WEBアプリ、スマホアプリ、デスクトップアプリなど幅広い開発に汎用的に対応できること。
4. 要件を満たし、UI・UXも含めプロダクトレベルで十分な開発品質であること。
5. 人間の指示や判断に誤りがあっても、裏にある課題や望みを考慮して訂正してくれること。

## MVPのスコープ
- 1〜2は満たす
- 3の汎用性は意識しつつも、まずはAndroidアプリ開発で動くようにする
- 4はUI・UX部分はやや落としても良いが、ビルドが通りロジックが正しいところは担保する
- 5は落とす

## MVP実装TODO

設計（`開発ループ設計.md`）は描けているが中身が未実装。優先度は A → B → C → D → E の順。F は A を書く過程で並行して決める。

### A. 司令塔（最優先）
- [ ] `orchestrator` skill（またはAgent） を新規作成 — ループ＋2ゲートを手順化。各工程skillを順に呼び、ゲートで止まる進行制御を書く（人間の窓口になるメインループ常駐の司令塔）
- [ ] ゲートの仕組みを定義 — ゲート①（計画確定）/ ゲート②（最終検証）で必ず止まり、承認/修正を取る手順を明記
- [ ] 割り込み質問の発火条件を定義（ゴール2の肝）— 「仕様の前提が崩れる重大発見」時のみゲート外でも人間に聞く、その判断基準を言語化

### B. 各工程Skillの補完
- [ ] `build` skill の中身を実装 — 現状frontmatterのみ。Android（Gradle）のビルド手順を記述
- [ ] `run-test` skill を新規作成 — ユニットテスト実行（`gradlew test` 等）
- [ ] `planning` skill をテンプレに用意 — 実装計画＋テスト計画を出す（グローバルには有るが project/ テンプレ内に無い）
- [ ] `verify` skill をテンプレに用意 — 検証手順（同上、テンプレ内に無い）

### C. 検証フェーズ（Workflow並列）
- [ ] 検証Workflowスクリプトを作成 — 観点を並列検証 → 各指摘を別agentで反証検証。MVPはゴール5を落とすため「クライアントの真なる望み」観点は外し、3観点（テストpass / 仕様 / 要件）に絞る
- [ ] 合否基準をゲート①の計画段階で確定する流れを orchestrator に組み込む（Workflowは人に聞けないため）

### D. Android開発の足場
- [ ] `ground-truth/` にAndroidサンプルプロジェクトを置く（現状空）— ループを実際に回す検証ターゲット
- [ ] `docs/環境と外部サービス.md` をAndroid向けに記載 — JDK / Android SDK / Gradle のバージョン・前提環境
- [ ] `project/CLAUDE.md` のプレースホルダーをAndroid開発用に具体化

### E. 通しテスト（E2E動作確認）
- [ ] 簡単なAndroid要件を1つ用意し、要望→interview→計画→ゲート①→実装→test→検証→ゲート② を実際に通しで回す
- [ ] 通した結果、ビルドが通る＋ロジックが正しいことを確認（ゴール4のMVP担保ライン）

### F. 未確定事項の決着（実装前に潰す）
- [ ] ゲートで人間が「修正」と言った時の差し戻し先（どの工程まで巻き戻すか）を決める
- [ ] 実装を subagent委任 vs メインで直接 やるか（タスクの重さで切り替え？）を決める
- [ ] 1ループ＝1機能の粒度、大きい要望をどう分割するかを決める